{
  "packages": ["packages/*", "packages/ethereum/*"],
  "version": "independent",
  "command": {
    "version": {
      "allowBranch": ["alpha", "beta", "rc", "master"]
    },
    "bootstrap": {
      "reject-cycles": true
    }
  }
}
